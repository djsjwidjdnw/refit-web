# ReFit web — Supabase (same project as the mobile app). Publishable/anon key only.
# Copy to .env.local and fill in. NEVER commit .env.local or any secret/service-role key.
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<publishable anon key>
